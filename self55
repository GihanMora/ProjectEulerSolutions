def pali(x):
    if str(x)==str(x)[::-1]:
        return True
def nl(y):
    c=0
    while c<50:
        c=c+1
        y=y+int(str(y)[::-1])
        if pali(y):
            return y
c=0
for i in range(1,10000):
    if not nl(i):
        c=c+1
print c
        
